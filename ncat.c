#include <stdio.h>
#include <string.h>
#include <stdlib.h>
#include <getopt.h>

struct FLAGS
{
	fprintf(stdout,"The Flags are: "
	"   -c  --stream   Sending th eoutput to the input stream to the input socket\n"
   "   -o  --option   copy output to specific file\n"
   "   -l  --Socket   makes a socket\n",
	progName);

int main(int argc, char *argv[]){




	return 0;
}

